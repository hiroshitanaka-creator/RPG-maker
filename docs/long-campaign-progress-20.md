# 第3章の連作接続と検査記録

> 履歴資料: 以下の進捗・未達・暫定値は記録時点の状態です。現在の実装・受入・残作業は [現在地](current-status.md) を参照してください。

以下は部分制作時点の履歴。現在は20連作80話を本番へ接続済み。最新の検査結果は [長編の実装記録](long-campaign-finish.md) を参照する。

2026年9月20日。制作中。正式目標は約60時間。以下の内容構成・名称・会話は設計者の提案であり、件数を所要時間の証明にはしない。

## 20話時点の変更

- 第3章の入口から既存の水路を再訪する4話を追加した。通し検査は対象章の入口状態を使えるようにし、従来の第2章入口と開始イベントの検査も維持した。
- 新たな2件の設置・回収と依存関係を台帳・手帳へ接続した。台帳は合計12件。既存R01〜R08を置き換えていない。
- 過去場面の見出しを「過去の記録」へ修正した。本文で示していない災害の時期へ全ての昔話を固定する表示をやめた。
- 20話時点では5連作、制作済み20区画の再訪80場面、120戦、20選択。区画定義80件のうち、未制作の話の区画を実装済みの内容へ数えない。本番の長編有効化はfalse。

## 実際の検査

| コマンド・条件 | 結果 |
|---|---|
| `godot --headless --path . --script res://tools/check_long_integration.gd -- --arc=w_seeds` の追加前 | exit 1。未実装の連作へ入れないためFAIL。構文エラーではない |
| 同じ検査の追加後。3人／4人・両選択・全4話 | exit 0。16経路・96戦・3,148マス。各進行地点の別インスタンスへの保存復帰、帰還再開、選択結果・手帳の一致も確認 |
| `godot --headless --path . --script res://tools/check_long_clues.gd` | exit 0。12件の本文・提示順・依存と、設置・回収前後の既読境界 |
| `godot --headless --path . --script res://tools/check_long_past.gd` | exit 0。5場面の過去を一続きで表示し、現在の戦闘を挟まない |
| `godot --headless --path . --script res://tools/check_long_ui.gd` | 見出し修正前exit 1、修正後exit 0。74画面。第3章の依頼一覧と現地、12件の手帳、5場面の過去を3人／4人で検査 |
| `godot --headless --path . --script res://tools/check_long_content.gd` | exit 1。全80話未完成と本番無効の2件。全体完成にはしない |

通し検査の入口拡張時にGDScriptの型推論エラーが1回出た。明示的なDictionary型へ修正して再実行し、上記16経路が完走した。型エラーの試行を初期FAILや成功した実行へ混ぜていない。

## 24話までの追加と検査

同じ第3章の廃坑へ4話を追加し、6連作24話・16件の設置と回収になった。追加連作 `c_statues` の通しは実装前exit 1。実装後は3人／4人・両選択の16経路・96戦・3,560マスでPASS。UIは92画面、既読境界は16件、過去の一括提示は6場面でPASS。

再実行を始めた際、前の実行と検査用セーブの名前が重なった。重複した実行は採用せず、検査を停止した。その後、セーブと結果ファイルを連作ID・プロセスIDで分離し、最初から単独実行した結果が上記のPASSである。人間の通常セーブは使っていない。

長い検査の結果には開始時のコード・データ・素材の版ID、カタログのSHA-256、検査コードのSHA-256を記録する。終了時にも一致を確認し、途中で入力が変わった実行を成功扱いにしない。実行時間は検査の経過秒であり、人間の所要時間へ換算しない。

## 未達

未制作12連作48話、各依頼の固有の観察・装置・経路・育成判断、全長編の通常操作による通し、長い保存の容量・時間が残る。人間の初見時間・主観はNOT_RUN。部分検査や台本件数を、人間が60時間遊べた証拠へ置き換えない。

CIはGitHubのコミット別実行を参照する。この記録だけで現在のHEADのCIを成功扱いにはしない。独立作業を続け、人間試遊待ちへは移さない。

## 28話までの追加と検査

教習所の4話を追加し、7連作28話・19件の設置と回収になった。習得と装着の違い、戦闘中に装着を変更できない制約を出来事に接続した。現在の戦闘の結果を、台詞だけで勝利したことにはしていない。

`s_dummy` の同じ通しコマンドは実装前exit 1、追加後exit 0。3人／4人・両選択で16経路・96戦・3,360マス・304回の別インスタンスへの保存復帰を確認した。検査の開始と終了で版ID・カタログ・検査コードのハッシュが一致した。検査実行の経過306.517秒は、人間のプレイ時間ではない。

UIは106画面、既読境界は19件、過去の一括提示は7場面でPASS。全80話の構造検査は、未完成と本番無効の2件でFAILのまま保持している。

## 32話までの追加と順不同の文面

教習所の庭の4話を追加し、8連作32話・22件の設置と回収になった。`s_garden` の追加前のFAILは同じ初期記録に保持している。追加後は16経路・96戦・3,092マス・304保存復帰でPASS。UIは120画面、既読境界は22件、過去の一括提示は8場面でPASS。

独立した連作を先に読んでいる場合にも話が合うよう、廃坑の終盤の話題紹介を初見前提の文面から修正した。手帳も、回収後に生まれた問いを「そのときの問い」として残す。すでに答えを読んだ問いを、現在の未達目標のように扱わないための変更である。`c_lamp` も同じ16経路・96戦・304保存復帰を再実行してPASSした。これを、人間がすべての順で理解できた証拠にはしない。
